# PolyBot Dependencies
# Install with: pip install -r requirements.txt

# Polymarket official client
py-clob-client>=0.34.0

# Web3 (let py-clob-client resolve compatible version)
web3>=6.0.0

# HTTP client (async)
httpx>=0.27.0

# API framework for dashboard
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Configuration
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0

# Retry logic
tenacity>=8.2.0

# Type hints
typing-extensions>=4.0.0

# Async HTTP (already covered by httpx, but explicit for agent swarm)
aiohttp>=3.9.0
